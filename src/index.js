// eslint-disable-next-line no-unused-vars
import _ from 'lodash';
import './style.css';
import './main.scss';

const addup = () => {
  const a = 2;
  const b = 2;

  return a + b;
};

module.exports = addup;